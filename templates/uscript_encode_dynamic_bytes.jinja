    BLen = Msg.{{ field.name }}.Length;
    Bytes.Length = Bytes.Length + BLen;
    Bytes[I++] = BLen;
    for (BIdx = 0; BIdx < BLen; ++BIdx)
    {
        Bytes[I++] = Msg.{{ field.name }}[BIdx];
    }
